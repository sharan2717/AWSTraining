name: demo-workflow

on: [push]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Sample
        run: npm install
      - name : deploy cdk
        run : cdk deploy